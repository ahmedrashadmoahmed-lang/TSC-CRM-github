# 🎯 دليل سريع: إعداد قاعدة البيانات

## الوضع الحالي
- لديك PostgreSQL 18 و PostgreSQL 16 مثبتين
- PostgreSQL 16 شغال حالياً
- نحتاج إنشاء قاعدة بيانات `erp_database`

---

## ✅ الحل الأسهل: استخدام pgAdmin

### الخطوة 1: افتح pgAdmin 4
- ابحث عن "pgAdmin 4" في قائمة Start
- أو من: `C:\Program Files\PostgreSQL\16\pgAdmin 4\bin\pgAdmin4.exe`

### الخطوة 2: أدخل كلمة المرور الرئيسية
- سيطلب منك Master Password
- أدخلها (هذه كلمة مرور pgAdmin نفسه، ليست كلمة مرور PostgreSQL)

### الخطوة 3: اتصل بـ PostgreSQL
- في الشجرة على اليسار، اضغط على **Servers**
- اضغط على **PostgreSQL 16** (أو PostgreSQL 18 إذا ظهر)
- سيطلب كلمة مرور PostgreSQL - أدخلها

### الخطوة 4: إنشاء قاعدة البيانات
1. اضغط بالزر الأيمن على **Databases**
2. اختر **Create → Database...**
3. في نافذة Create Database:
   - **Database:** اكتب `erp_database`
   - **Owner:** اترك `postgres`
   - **Encoding:** اترك `UTF8`
4. اضغط **Save**

### الخطوة 5: تأكد من الإنشاء
- يجب أن ترى `erp_database` في قائمة Databases
- ✅ تم!

---

## 🔧 بديل: استخدام PowerShell

إذا كنت تفضل PowerShell، افتحه كـ **Administrator** واكتب:

```powershell
# للـ PostgreSQL 16
& "C:\Program Files\PostgreSQL\16\bin\psql.exe" -U postgres -c "CREATE DATABASE erp_database;"

# أو للـ PostgreSQL 18 (إذا كان مثبت)
& "C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -c "CREATE DATABASE erp_database;"
```

سيطلب كلمة المرور - أدخلها.

---

## 📋 بعد إنشاء قاعدة البيانات

### الخطوة التالية: تحديث ملف .env

افتح ملف `.env` في مجلد المشروع وعدّل:

```env
DATABASE_URL="postgresql://postgres:كلمة_المرور_هنا@localhost:5432/erp_database?schema=public"
```

استبدل `كلمة_المرور_هنا` بكلمة مرور PostgreSQL الفعلية.

---

## ✅ اختبار الاتصال

بعد إنشاء القاعدة وتحديث .env، جرب:

```powershell
npm run db:push
```

إذا نجح، معناها قاعدة البيانات جاهزة! 🎉

---

## ❓ مشاكل شائعة

### المشكلة: "password authentication failed"
**الحل:** تأكد من كلمة المرور في `.env` صحيحة

### المشكلة: "database does not exist"
**الحل:** ارجع لـ pgAdmin وتأكد من إنشاء `erp_database`

### المشكلة: "connection refused"
**الحل:** تأكد أن خدمة PostgreSQL شغالة:
```powershell
Get-Service postgresql-x64-16
```

إذا Status = Stopped، شغلها:
```powershell
Start-Service postgresql-x64-16
```

---

**أخبرني عندما تنتهي من إنشاء قاعدة البيانات!** 🚀
