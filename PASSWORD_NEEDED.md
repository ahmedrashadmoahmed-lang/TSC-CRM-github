# 🔑 كلمة مرور PostgreSQL مطلوبة

## المشكلة
- PostgreSQL شغال على port **5433** ✅
- ملف `.env` معدّل ✅
- كلمة المرور غلط ❌

## الحل

**أدخل كلمة مرور PostgreSQL الصحيحة:**

الكلمة اللي أدخلتها عند تثبيت PostgreSQL.

## الخطوات التالية

1. افتح ملف `.env`
2. عدّل السطر:
```
DATABASE_URL="postgresql://postgres:كلمة_المرور_الصحيحة@localhost:5433/erp_database?schema=public"
```

3. احفظ الملف

4. شغّل الأوامر التالية:

```powershell
# إنشاء قاعدة البيانات
$env:PGPASSWORD='كلمة_المرور_الصحيحة'
& "C:\Program Files\PostgreSQL\16\bin\psql.exe" -U postgres -p 5433 -c "CREATE DATABASE erp_database;"

# إعداد قاعدة البيانات
npm run db:push

# تشغيل النظام
npm run dev
```

---

**ملاحظة:** استبدل `كلمة_المرور_الصحيحة` بكلمة المرور الفعلية!
