# 🚀 تثبيت وتشغيل النظام - دليل كامل

## ✅ الخطوة 1: تثبيت PostgreSQL

اتبع الخطوات في ملف `POSTGRESQL_SETUP.md`

## ✅ الخطوة 2: التثبيت التلقائي (الطريقة السهلة)

### للويندوز:
```bash
# شغل ملف التثبيت التلقائي
setup.bat
```

سيقوم البرنامج بـ:
1. ✅ فحص Node.js
2. ✅ تثبيت المكتبات
3. ✅ إنشاء ملف .env
4. ✅ إعداد Prisma
5. ✅ إنشاء قاعدة البيانات

**مهم:** عند توقف البرنامج، افتح ملف `.env` وضع كلمة مرور PostgreSQL!

---

## 📋 الخطوة 3: التثبيت اليدوي (إذا فشل التلقائي)

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. إنشاء ملف .env
```bash
copy .env.example .env
```

### 3. تعديل .env
افتح `.env` وغير:
```env
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/erp_database?schema=public"
```
غير `YOUR_PASSWORD` بكلمة مرور PostgreSQL

### 4. إعداد قاعدة البيانات
```bash
# إنشاء قاعدة البيانات (في pgAdmin أو psql)
CREATE DATABASE erp_database;

# إنشاء Prisma Client
npx prisma generate

# إنشاء الجداول
npx prisma db push
```

---

## 🎯 الخطوة 4: تشغيل النظام

```bash
npm run dev
```

افتح المتصفح: **http://localhost:3000**

---

## 📊 الصفحات المتاحة

| الصفحة | الرابط |
|--------|--------|
| الرئيسية | `/` |
| لوحة التحكم | `/dashboard-example` |
| العملاء | `/examples/customers` |
| المنتجات | `/products` |
| الموظفين | `/employees` |
| التقارير | `/reports` |
| اختبار النموذج | `/test-form` |

---

## 🛠️ أوامر مفيدة

```bash
# تشغيل وضع التطوير
npm run dev

# بناء للإنتاج
npm run build

# تشغيل الإنتاج
npm start

# فتح Prisma Studio (لإدارة البيانات)
npm run db:studio

# إعادة تعيين قاعدة البيانات
npm run db:reset

# إعداد كامل من الصفر
npm run setup
```

---

## ❓ حل المشاكل

### "Cannot connect to database"
```bash
# تأكد أن PostgreSQL يعمل
# في Windows: افتح Services وابحث عن postgresql

# تحقق من .env
# تأكد أن كلمة المرور صحيحة
```

### "Module not found"
```bash
npm install
```

### "Prisma Client not generated"
```bash
npx prisma generate
```

### "Port 3000 already in use"
```bash
# أوقف التطبيق الآخر أو غير PORT في .env
PORT=3001
```

---

## 📁 هيكل المشروع

```
antigravity/
├── prisma/
│   └── schema.prisma          # قاعدة البيانات
├── src/
│   ├── app/                   # الصفحات
│   ├── components/            # المكونات (24 مكون)
│   ├── services/              # الخدمات (10 خدمات)
│   ├── lib/                   # المكتبات
│   ├── utils/                 # الأدوات
│   └── hooks/                 # React Hooks
├── .env                       # الإعدادات
├── package.json               # المكتبات
├── next.config.js             # إعدادات Next.js
└── setup.bat                  # التثبيت التلقائي
```

---

## 🎉 بعد التثبيت

1. ✅ افتح Prisma Studio لإضافة بيانات تجريبية:
   ```bash
   npm run db:studio
   ```

2. ✅ جرب الصفحات المختلفة

3. ✅ اقرأ التوثيق في:
   - `ULTIMATE_SUMMARY.md` - ملخص شامل
   - `src/components/COMPONENTS_EXTENDED.md` - توثيق المكونات
   - `src/services/README.md` - توثيق الخدمات

---

## 📞 محتاج مساعدة؟

إذا واجهت أي مشكلة:
1. تحقق من ملف `POSTGRESQL_SETUP.md`
2. تحقق من ملف `.env`
3. تأكد أن PostgreSQL يعمل
4. جرب `npm run setup`

---

**جاهز للعمل! 🚀**
