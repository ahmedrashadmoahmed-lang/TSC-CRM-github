# 🚀 خطوات سريعة لإعداد قاعدة البيانات

## الخطوة 1️⃣: تثبيت PostgreSQL

### لديك ملف التثبيت جاهز! 🎉
ملف `postgresql-18.1-1-windows-x64.exe` موجود في المجلد.

### خطوات التثبيت:
1. **شغّل الملف** `postgresql-18.1-1-windows-x64.exe` (دبل كليك)
2. **اتبع المعالج:**
   - اضغط **Next** → **Next**
   - **Password**: اختر كلمة مرور **واحفظها!** (مثال: `postgres123`)
   - **Port**: اترك `5432`
   - **Locale**: اترك الافتراضي
   - اضغط **Next** حتى النهاية
3. **انتظر التثبيت** (5-10 دقائق)

---

## الخطوة 2️⃣: تحديث ملف .env

✅ **تم تحديث الملف تلقائياً!** لكن **يجب تغيير كلمة المرور**:

افتح ملف `.env` وغيّر `your_password` بكلمة المرور التي اخترتها:

```env
DATABASE_URL="postgresql://postgres:كلمة_المرور_هنا@localhost:5432/erp_database?schema=public"
```

**مثال:**
```env
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/erp_database?schema=public"
```

---

## الخطوة 3️⃣: تشغيل سكريبت الإعداد التلقائي

بعد تثبيت PostgreSQL وتحديث `.env`، افتح **PowerShell** في مجلد المشروع ونفذ:

```powershell
# طريقة 1: تشغيل السكريبت التلقائي (الأسهل)
.\setup-database.ps1
```

**أو يدوياً:**

```powershell
# طريقة 2: الخطوات اليدوية
# 1. إنشاء قاعدة البيانات
psql -U postgres -c "CREATE DATABASE erp_database;"

# 2. تثبيت المكتبات
npm install

# 3. إنشاء Prisma Client
npx prisma generate

# 4. إنشاء الجداول
npx prisma db push

# 5. تعبئة بيانات تجريبية (اختياري)
node prisma/seed.js
```

---

## الخطوة 4️⃣: التحقق من النجاح

```powershell
# فتح Prisma Studio لمشاهدة البيانات
npx prisma studio
```

يجب أن يفتح متصفح على `http://localhost:5555` وترى **17 جدول**! ✅

---

## الخطوة 5️⃣: تشغيل المشروع

```powershell
npm run dev
```

افتح المتصفح: `http://localhost:3000` 🎉

---

## 🔧 إذا واجهت مشكلة

### المشكلة: "psql: command not found"

**الحل:**
1. افتح **System Properties** (Win + Pause)
2. **Advanced** → **Environment Variables**
3. في **System Variables**، اختر `Path` → **Edit**
4. اضغط **New** وأضف:
   ```
   C:\Program Files\PostgreSQL\18\bin
   ```
5. اضغط **OK** وأعد تشغيل PowerShell

### المشكلة: "password authentication failed"

**الحل:** تأكد أن كلمة المرور في `.env` صحيحة

### المشكلة: "database does not exist"

**الحل:**
```powershell
psql -U postgres
CREATE DATABASE erp_database;
\q
```

---

## 📊 ما الذي سيتم إنشاؤه؟

### 17 جدول في قاعدة البيانات:

#### الجداول الأساسية (5):
- ✅ `tenants` - الشركات
- ✅ `users` - المستخدمين
- ✅ `customers` - العملاء
- ✅ `suppliers` - الموردين
- ✅ `products` - المنتجات

#### جداول المعاملات (4):
- ✅ `invoices` - الفواتير
- ✅ `invoice_items` - بنود الفواتير
- ✅ `purchase_orders` - أوامر الشراء
- ✅ `purchase_order_items` - بنود أوامر الشراء

#### الموارد البشرية (2):
- ✅ `employees` - الموظفين
- ✅ `payroll` - الرواتب

#### المحاسبة (3):
- ✅ `accounts` - الحسابات
- ✅ `journal_entries` - القيود اليومية
- ✅ `journal_lines` - بنود القيود

#### النظام (3):
- ✅ `notifications` - الإشعارات
- ✅ `audit_logs` - سجلات التدقيق
- ✅ `inventory_movements` - حركات المخزون

---

## 🎯 الملخص السريع

```powershell
# 1. ثبت PostgreSQL من الملف الموجود
# 2. حدث كلمة المرور في .env
# 3. نفذ:
.\setup-database.ps1

# أو يدوياً:
npm install
npx prisma generate
npx prisma db push
npm run dev
```

---

## 📞 محتاج مساعدة؟

راسلني بـ:
- رسالة الخطأ
- الخطوة التي وقفت عندها
- لقطة شاشة

وسأساعدك فوراً! 💪🚀
