# 🗄️ دليل إعداد قاعدة البيانات - PostgreSQL

## ✅ الخطوة 1: تثبيت PostgreSQL

### إذا لم تقم بالتثبيت بعد:
1. شغّل ملف `postgresql-18.1-1-windows-x64.exe` الموجود في المجلد
2. اتبع معالج التثبيت:
   - **Password**: اختر كلمة مرور قوية (مثلاً: `postgres123`) ⚠️ **احفظها!**
   - **Port**: اترك `5432` (الافتراضي)
   - **Locale**: اختر `Default locale`
3. انتظر حتى ينتهي التثبيت

### إذا قمت بالتثبيت مسبقاً:
تأكد أن PostgreSQL يعمل:
```powershell
# افتح PowerShell واكتب:
psql --version
```

يجب أن ترى: `psql (PostgreSQL) 18.1`

---

## ✅ الخطوة 2: إنشاء قاعدة البيانات

### الطريقة الأولى: باستخدام pgAdmin (الأسهل)

1. افتح **pgAdmin 4** من قائمة Start
2. أدخل كلمة المرور التي اخترتها
3. من الشجرة على اليسار: **Servers** → **PostgreSQL 18** → **Databases**
4. كليك يمين على **Databases** → **Create** → **Database**
5. في خانة **Database**: اكتب `erp_database`
6. اضغط **Save**

### الطريقة الثانية: باستخدام Command Line

```powershell
# افتح PowerShell كـ Administrator واكتب:
psql -U postgres

# بعد إدخال كلمة المرور، اكتب:
CREATE DATABASE erp_database;

# للتحقق من إنشاء القاعدة:
\l

# للخروج:
\q
```

---

## ✅ الخطوة 3: تحديث ملف .env

ملف `.env` تم تحديثه تلقائياً! لكن **يجب عليك تغيير كلمة المرور**:

افتح ملف `.env` وغيّر `your_password` بكلمة المرور الحقيقية:

```env
DATABASE_URL="postgresql://postgres:كلمة_المرور_الحقيقية@localhost:5432/erp_database?schema=public"
```

**مثال:**
```env
DATABASE_URL="postgresql://postgres:postgres123@localhost:5432/erp_database?schema=public"
```

---

## ✅ الخطوة 4: إنشاء الجداول في قاعدة البيانات

افتح PowerShell في مجلد المشروع ونفذ الأوامر التالية:

```powershell
# 1. تثبيت المكتبات (إذا لم تكن مثبتة)
npm install

# 2. إنشاء Prisma Client
npx prisma generate

# 3. إنشاء الجداول في قاعدة البيانات
npx prisma db push

# 4. (اختياري) تعبئة البيانات التجريبية
node prisma/seed.js
```

---

## ✅ الخطوة 5: التحقق من نجاح الإعداد

### 1. فتح Prisma Studio:
```powershell
npx prisma studio
```

يجب أن يفتح متصفح على `http://localhost:5555` وترى جميع الجداول:
- ✅ tenants
- ✅ users
- ✅ customers
- ✅ suppliers
- ✅ products
- ✅ invoices
- ✅ purchase_orders
- ✅ employees
- ✅ payroll
- ✅ accounts
- ✅ journal_entries
- ✅ notifications
- ✅ audit_logs

### 2. تشغيل المشروع:
```powershell
npm run dev
```

افتح المتصفح: `http://localhost:3000`

---

## 🔧 حل المشاكل الشائعة

### ❌ خطأ: "password authentication failed"

**السبب:** كلمة المرور في `.env` غير صحيحة

**الحل:**
1. تأكد من كلمة المرور في ملف `.env`
2. جرب تسجيل الدخول يدوياً:
   ```powershell
   psql -U postgres -d erp_database
   ```

### ❌ خطأ: "database does not exist"

**السبب:** لم يتم إنشاء قاعدة البيانات

**الحل:**
```powershell
psql -U postgres
CREATE DATABASE erp_database;
\q
```

### ❌ خطأ: "Port 5432 already in use"

**السبب:** PostgreSQL لا يعمل أو هناك خدمة أخرى تستخدم نفس المنفذ

**الحل:**
1. افتح **Services** (اضغط `Win + R` واكتب `services.msc`)
2. ابحث عن **postgresql-x64-18**
3. كليك يمين → **Restart**

### ❌ خطأ: "psql: command not found"

**السبب:** PostgreSQL غير مضاف إلى PATH

**الحل:**
1. افتح **System Properties** → **Advanced** → **Environment Variables**
2. في **System Variables**، ابحث عن `Path`
3. اضغط **Edit** → **New**
4. أضف: `C:\Program Files\PostgreSQL\18\bin`
5. اضغط **OK** وأعد تشغيل PowerShell

### ❌ خطأ: "Prisma schema loading failed"

**الحل:**
```powershell
npx prisma generate
npx prisma db push --force-reset
```

---

## 📊 هيكل قاعدة البيانات

تم إنشاء **15 جدول** في قاعدة البيانات:

### الجداول الأساسية:
1. **tenants** - الشركات/المستأجرين
2. **users** - المستخدمين
3. **customers** - العملاء
4. **suppliers** - الموردين
5. **products** - المنتجات

### جداول المعاملات:
6. **invoices** - الفواتير
7. **invoice_items** - بنود الفواتير
8. **purchase_orders** - أوامر الشراء
9. **purchase_order_items** - بنود أوامر الشراء

### جداول الموارد البشرية:
10. **employees** - الموظفين
11. **payroll** - الرواتب

### جداول المحاسبة:
12. **accounts** - الحسابات
13. **journal_entries** - القيود اليومية
14. **journal_lines** - بنود القيود

### جداول النظام:
15. **notifications** - الإشعارات
16. **audit_logs** - سجلات التدقيق
17. **inventory_movements** - حركات المخزون

---

## 🎉 تم الإعداد بنجاح!

إذا وصلت لهنا بدون أخطاء، فقاعدة البيانات جاهزة للاستخدام! 🚀

### الخطوات التالية:
1. ✅ تشغيل المشروع: `npm run dev`
2. ✅ فتح المتصفح: `http://localhost:3000`
3. ✅ استكشاف النظام

---

## 📞 محتاج مساعدة؟

إذا واجهت أي مشكلة، أرسل لي:
1. رسالة الخطأ الكاملة
2. الخطوة التي وقفت عندها
3. لقطة شاشة إن أمكن

وسأساعدك فوراً! 💪
